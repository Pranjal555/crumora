/**
 * Edit these values for your deploy — no build step required.
 * WhatsApp: country code + number, digits only (no + or spaces).
 */
window.CRUMORA_CONFIG = {
  brandName: "Crumora Patisserie",
  tagline: "West Delhi's premium dessert studio",
  whatsappCountryCode: "91",
  whatsappNumber: "8700432133",
  /** Shown in WhatsApp message greetings and on Contact. */
  whatsappContactName: "Deepanshu Pahuja",
  instagramUrl: "https://www.instagram.com/crumora_patisserie/",
  /** If you use a project URL like https://user.github.io/crumo/, leave this as "" and use relative links (default). */
  basePath: "",
};
